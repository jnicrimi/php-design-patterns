{
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.md": "markdownlint --ignore node_modules --ignore tools --ignore vendor .",
    "**/*.{yaml,yml,yamllint}": "yamllint --strict ."
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "husky": "^9.0.10",
    "lint-staged": "^15.2.1"
  }
}
